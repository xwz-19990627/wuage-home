# v0.2.0-rc.1 归档（2026-08-22）

## 一句话总结

v0.2 系列（财务健康）第一个 rc：首页 Dashboard 优化 + 全局 UI 质感升级 + 版本命名纠正。

## 交付

- 首页：月份联动修复（选历史月有数据）、日均花费、环比 + 同比（Pill，悬停显示基准）、移除近 7 日柱图、饼图放大 + 图例两列。
- AI 记账：手动录入默认展开，AI / 手动卡片分层。
- 账单：分类彩色 Pill；筛选默认「全部」（不再空白）。
- 全局：Toast 替换 alert（删除确认除外）；空状态引导；Tab 上移动效。
- 后端：dashboard 新增 yoy_pct / last_year_* / days_elapsed 等字段；近 7 日窗口随所选月份。
- 版本规划：无 v2.0；v0.2 系列范围定稿（性质 / 预算 / 负债表 / 理财），产品决策已记录；删除旧 v0.2.0 里程碑 tag。

## 关键修复

- 首页月份 offset 正负号反了 → 选历史月显示无数据（改为 -i）。
- 账单「全部」下拉被 fillSelect 洗成空白 → 用对象选项带文案。

## 验收

- 用户页面验收通过（首页 / AI记账 / 账单 / 家庭 4 Tab）。

## 下一步（规划中）

- rc.2 支出性质统计（分类挂固定/必要/非必要标签，记账自动归类）→ 写 spec 后开工。
